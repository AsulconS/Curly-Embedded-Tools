<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>com.asulcons.Curly-Embedded-Tools</id>

    <!-- Public plugin name should be written in Title Case.
     Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-name -->
    <name>Curly-Embedded-Tools</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor url="https://github.com/AsulconS" email="adrian.r.bedregal@gmail.com">AsulconS</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-description -->
    <description><![CDATA[
        Bare-metal ARMv8 (AArch64) embedded development tooling.<br>
        Adds custom language support for the three file kinds a QEMU + <code>gdb-multiarch</code> workflow
        revolves around:
        <ul>
          <li><b>ARMv8 assembly</b> (<code>.s</code>, <code>.S</code>, <code>.asm</code>) &mdash; lexing, parsing,
              syntax highlighting, formatting, completion, inspections and intentions.</li>
          <li><b>GDB scripts</b> (<code>.gdbinit</code>, <code>*.gdb</code>) &mdash; command completion,
              block structure checking and folding.</li>
          <li><b>GNU linker scripts</b> (<code>.ld</code>, <code>.lds</code>) &mdash; section/region analysis
              with navigation from <code>&gt; REGION</code> to its <code>MEMORY</code> declaration.</li>
        </ul>
    ]]></description>

    <!-- Only core language APIs are used, so the plugin is compatible with every IntelliJ-based IDE,
         RustRover included. -->
    <depends>com.intellij.modules.lang</depends>

    <content>
        <module name="Curly.Embedded.Tools.shared" loading="required"/>
        <module name="Curly.Embedded.Tools.language" loading="required"/>
        <module name="Curly.Embedded.Tools.frontend"/>
        <module name="Curly.Embedded.Tools.backend"/>
    </content>
</idea-plugin>
